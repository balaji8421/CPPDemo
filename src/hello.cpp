#include <iostream>
//#include <thread>
//#include <chrono>
#include "hello.h"

void hello(){
    std::cout << "Hello, World!" << std::ends;
   // std::this_thread::sleep_for(std::chrono::hours(1));
}